<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:d="http://xamarin.com/schemas/2014/forms/design"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:controls="clr-namespace:ComicsShelf.Controls"
   mc:Ignorable="d"
   Title="{Binding ComicFile.ComicFile.FullText}" 
   Shell.NavBarIsVisible="False"
   x:Class="ComicsShelf.ComicFiles.ReadingView">
   <ContentPage.Content>

      <!--
      <CollectionView 
         VerticalOptions="StartAndExpand" HorizontalOptions="StartAndExpand" 
         ItemsSource="{Binding ComicFile.Pages}" >
         <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="5" />
         </CollectionView.ItemsLayout>
         <CollectionView.ItemTemplate>
            <DataTemplate>
               <Grid ColumnSpacing="5" RowSpacing="5">
                  <Grid.RowDefinitions>
                     <RowDefinition Height="145"></RowDefinition>
                  </Grid.RowDefinitions>
                  <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="222"></ColumnDefinition>
                  </Grid.ColumnDefinitions>
                  <Image Source="{Binding Path}" Aspect="AspectFit"/>
               </Grid>
            </DataTemplate>
         </CollectionView.ItemTemplate>
      </CollectionView>
      -->

      <CarouselView HorizontalOptions="Fill" VerticalOptions="Fill" ItemsSource="{Binding ComicFile.Pages}">
         <CarouselView.ItemsLayout>
            <GridItemsLayout Orientation="Horizontal" SnapPointsType="Mandatory" SnapPointsAlignment="Center"/>
         </CarouselView.ItemsLayout>
         <CarouselView.ItemTemplate>
            <DataTemplate>
               <controls:PageView ImagePath="{Binding Path}" ImageSize="{Binding PageSize}" ImageLoaded="True" ></controls:PageView>
            </DataTemplate>
         </CarouselView.ItemTemplate>
      </CarouselView>

   </ContentPage.Content>
</ContentPage>