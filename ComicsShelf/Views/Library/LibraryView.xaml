<?xml version="1.0" encoding="utf-8" ?>
<controls:BasePage 
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:controls="clr-namespace:ComicsShelf.Helpers.Controls"
   x:Class="ComicsShelf.Views.Library.LibraryView"
   x:Name="MyPage">
   <controls:BasePage.Content>
      <StackLayout VerticalOptions="CenterAndExpand" Spacing="40" >

         <StackLayout Spacing="5">
            <Label Text="{controls:Translate Text=LIBRARY_MAIN_LABEL}" HorizontalOptions="Center" FontSize="Large" />
            <Label Text="{controls:Translate Text=LIBRARY_UNDEFINED_FOLDER_MESSAGE}" IsVisible="{Binding Data.HasntLibraries}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontAttributes="Bold" />
            <controls:PrimaryButton Text="{controls:Translate Text=LIBRARY_SELECT_FOLDER_COMMAND}" Command="{Binding AddLibraryTappedCommand}" HorizontalOptions="Center" />
         </StackLayout>

         <controls:RepeaterView ItemsSource="{Binding Data.Libraries}" HorizontalOptions="CenterAndExpand" >
            <controls:RepeaterView.ItemTemplate>
               <DataTemplate>
                  <StackLayout Orientation="Horizontal" >
                     <Label Text="{Binding LibraryText}" HorizontalOptions="StartAndExpand" VerticalOptions="Center" FontAttributes="Bold"></Label>
                     <controls:PrimaryButton 
                        Text="{controls:Translate Text=LIBRARY_REMOVE_LIBRARY_COMMAND}" 
                        Command="{Binding BindingContext.RemoveLibraryTappedCommand, Source={x:Reference MyPage}}"
                        CommandParameter="{Binding .}"
                        HorizontalOptions="End">
                     </controls:PrimaryButton>
                  </StackLayout>
               </DataTemplate>
            </controls:RepeaterView.ItemTemplate>
         </controls:RepeaterView>

         <StackLayout Spacing="5" >
            <Label Text="{controls:Translate Text=LIBRARY_SUGGESTION_MESSAGE}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Micro" />
            <Label Text="www.comicbookplus.com" HorizontalOptions="Center" FontSize="Micro" FontAttributes="Bold" TextColor="{StaticResource colorAccent}" >
               <Label.GestureRecognizers>
                  <TapGestureRecognizer Command="{Binding LinkTappedCommand}" />
               </Label.GestureRecognizers>
            </Label>
         </StackLayout>

      </StackLayout>
   </controls:BasePage.Content>
</controls:BasePage>