<?xml version="1.0" encoding="utf-8" ?>
<controls:BasePage 
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:controls="clr-namespace:ComicsShelf.Helpers.Controls"
   x:Class="ComicsShelf.Views.Library.LibraryView"
   x:Name="MyPage">
   <controls:BasePage.Content>
      <StackLayout VerticalOptions="CenterAndExpand" Padding="10,80" Spacing="100" >

         <StackLayout Spacing="10">
            <Label Text="{controls:Translate Text=LIBRARY_MAIN_LABEL}" HorizontalOptions="Center" FontSize="Large" />
            <Label Text="{Binding Data.LibraryPath}" IsVisible="{Binding Data.IsLibraryDefined}" HorizontalOptions="Center" FontAttributes="Bold" />
            <Label Text="{controls:Translate Text=LIBRARY_UNDEFINED_FOLDER_MESSAGE}" IsVisible="{Binding Data.IsLibraryEmpty}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontAttributes="Bold" />
            <Button Text="{controls:Translate Text=LIBRARY_SELECT_FOLDER_COMMAND}" Command="{Binding AddLibraryTappedCommand}" HorizontalOptions="Center" />
         </StackLayout>

         <ListView ItemsSource="{Binding Data.Libraries}" HasUnevenRows="True" >
            <ListView.ItemTemplate>
               <DataTemplate>
                  <ViewCell>
                     <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding LibraryText}" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand"></Label>
                        <Button 
                           Command="{Binding BindingContext.RemoveLibraryTappedCommand, Source={x:Reference MyPage}}"
                           CommandParameter="{Binding .}"
                           Text="[DEL]" HorizontalOptions="End">
                        </Button>
                     </StackLayout>                     
                  </ViewCell>
               </DataTemplate>
            </ListView.ItemTemplate>
         </ListView>

         <StackLayout Spacing="5" >
            <Label Text="{controls:Translate Text=LIBRARY_SUGGESTION_MESSAGE}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Micro" />
            <Label Text="www.comicbookplus.com" HorizontalOptions="Center" FontSize="Micro" FontAttributes="Bold" TextColor="{StaticResource colorAccent}" >
               <Label.GestureRecognizers>
                  <TapGestureRecognizer Command="{Binding LinkTappedCommand}" />
               </Label.GestureRecognizers>
            </Label>
         </StackLayout>

      </StackLayout>
   </controls:BasePage.Content>
</controls:BasePage>