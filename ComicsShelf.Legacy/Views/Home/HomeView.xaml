<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage  
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:home="clr-namespace:ComicsShelf.Views.Home"
   xmlns:controls="clr-namespace:ComicsShelf.Helpers.Controls" 
   x:Class="ComicsShelf.Views.Home.HomeView" 
   BackgroundColor="{StaticResource colorBackground}"
   x:Name="MyHomeView"  
   ItemsSource="{Binding Data.Libraries}">
   <TabbedPage.ToolbarItems>
      <ToolbarItem Order="Primary" Text="{controls:Translate Text=HOME_LIBRARY_MENU}" Command="{Binding OpenLibraryCommand}" >
         <ToolbarItem.Icon>
            <OnPlatform x:TypeArguments="FileImageSource">
               <On Platform="Android" Value="icon_Library.png"></On>
               <On Platform="IOS" Value="icon_Library.png"></On>
               <On Platform="UWP" Value="Assets/icon_Library.png"></On>
            </OnPlatform>
         </ToolbarItem.Icon>
      </ToolbarItem>
   </TabbedPage.ToolbarItems>
   <TabbedPage.ItemTemplate>
      <DataTemplate>
         <home:LibraryPage x:Name="MyLibraryContent"  Title="{Binding Text}" />
      </DataTemplate>   
   </TabbedPage.ItemTemplate>
</TabbedPage>